(function (Drupal) {
  'use strict';

  // Reset: Do not modify DeepChat or form fields. Let ai_chatbot initialize
  // and handle the chat UI completely by itself.
  Drupal.behaviors.canvasgovAssistantWiring = {
    attach() {
      // Intentionally left blank.
    }
  };

})(Drupal);
